pipeline {
    agent {
        docker { image 'node:latest' } }
            stages {
                stage('build') {
                    steps {
                        sh 'npm --version'
            }
                stage ('deploy') {
                    steps {
                        sh ‘echo “Run some lints”’
            }
}
}